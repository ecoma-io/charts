{{- if index .Values "cert-manager" "webhook" "autoscaling" "enabled" }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: "{{ .Release.Name }}-webhook"
  namespace: {{ .Release.Namespace }}  
  annotations:
    "helm.sh/hook": post-install
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: "{{ .Release.Name }}-webhook"
  minReplicas: {{ index .Values "cert-manager" "webhook" "autoscaling" "minReplicas" }}
  maxReplicas: {{ index .Values "cert-manager" "webhook" "autoscaling" "maxReplicas" }}
  metrics:
    {{- if index .Values "cert-manager" "webhook" "autoscaling" "targetMemory" }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ index .Values "cert-manager" "webhook" "autoscaling" "targetMemory" }}
    {{- end }}
    {{- if index .Values "cert-manager" "webhook" "autoscaling" "targetCPU" }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ index .Values "cert-manager" "webhook" "autoscaling" "targetCPU" }}
    {{- end }}
{{- end }}
